defmodule T3CloneElixir.OpenrouterGeneratorTest do
  use ExUnit.Case

  test "hello/0 returns 'Hello World from Python'" do
    assert T3CloneElixir.OpenrouterGenerator.hello() == "Hello World from Python"
  end
end
